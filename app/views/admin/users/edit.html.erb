<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
    <h2 class="m-4">会員情報の編集</h2>
    <%= form_with model: @user, url: admin_user_path(@user), method: :patch, local: true do |f| %>
      <%= render 'layouts/errors', resource: @user %>
      <%# adminはアドレスの閲覧のみ %>
      <div class="form-group">
        <%= f.label :email, "メールアドレス" %>
        <%= f.text_field :email, value: @user.email, class: "form-control", disabled: true %>
      </div>
      <%# アカウント名編集 %>
      <div class="form-group">
        <%= f.label :name, "アカウント名" %>
        <%= f.text_field :name, class: "form-control name" %>
      </div>
      <%# 自己紹介編集 %>
      <div class="form-group">
        <%= f.label :introduction, "自己紹介" %>
        <%= f.text_area :introduction, class: "form-control introduction" %>
      </div>
      <%# プロフィール画像編集 %>
      <div class="form-group">
        <%= f.label :profile_image, "プロフィール画像" %><br>
        <%= f.file_field :profile_image %>
      </div>
      <%# adminユーザーのみ、ステータスの変更が可能。ステータスを退会済みにした場合、ユーザーはログイン不可。%>
      <div class="form-group">
        <%= f.label :users_status, "会員ステータス" %>
        <%= f.select :status, User.statuses.keys.map { |k| [I18n.t("enums.user.status.#{k}"), k] } %>
      </div>
      <%# 更新処理 %>
      <div class="form-group m-4">
        <%= f.submit "プロフィール情報を更新", class: 'btn btn-sm', style: "background-color: #66cdaa ; color: white;" %>
      </div>
      <%# 戻るボタン %>
      <div class="form-group m-4">
        <%= link_to "<<会員情報詳細ページへ戻る", admin_user_path(@user), class: 'btn btn-sm btn-secondary' %>
      </div>
    <% end %>
  </div>
</div>