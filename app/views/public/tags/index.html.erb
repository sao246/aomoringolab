
<div class='container my-4'>
  <div class='row justify-content-center'>
    <div class='col-md-8 offset-md-1'>
      <h4 class="m-4" style="border-bottom: 2px solid #ccc; padding-bottom: 8px;">タグ: <%= params[:tag_name] %> の投稿</h4>
        <% if @posts.present? %>
          <%# タグがついている投稿を一覧で表示するため一覧表示テンプレートを呼び出す %>
          <%= render 'public/posts/index', posts: @posts %>
        <% else %>
          <p>このタグに該当する投稿はまだありません。</p>
        <% end %>
    </div>
  </div>
</div>