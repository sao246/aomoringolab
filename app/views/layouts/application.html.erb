<!DOCTYPE html>
<html>
  <head>
    <title>Aomoringolab</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/e3debef22c.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body style="padding-top: 80px;">
  <%# header、エラーメッセージの出力追加 2025/04/08%>
    <%= render 'layouts/header' %> 
    <% flash.each do |key, message| %>
      <div class="flash <%= key %>" id="flash-message">
        <%= message %>
      </div>
    <% end %>
    <%# 検索ボックスの追加 2025/04/14%>
    <div class="search-form" style="display: flex; justify-content: left; flex-wrap: wrap; margin-top: 20px;">
      <%= render 'public/search/form' %>
    </div> 
    <%= yield %>
  </body>
</html>