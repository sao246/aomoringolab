<!DOCTYPE html>
<html>
  <head>
    <title>Aomoringolab</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/e3debef22c.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body style="padding-top: 80px; margin: 0; font-family: sans-serif; background-image: url('/your-background.jpg'); background-size: cover; background-position: center; background-repeat: no-repeat; background-attachment: fixed;">
    <%= render 'layouts/header' %> 

    <% flash.each do |key, message| %>
      <div class="flash <%= key %>" id="flash-message" 
           style="width: 90%; max-width: 1200px; margin: 20px auto; padding: 12px; border-radius: 8px; text-align: center; background-color: rgba(255,255,255,0.9);">
        <%= message %>
      </div>
    <% end %>

    <div class="main-content" style="width: 100%; max-width: 1200px; margin: 0 auto; padding: 20px; background-color: rgba(255,255,255,0.95); border-radius: 8px;">
      <%= yield %>
    </div>
  </body>
</html>
